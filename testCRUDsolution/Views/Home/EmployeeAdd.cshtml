@model testCRUDsolution.Models.Employee
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title>Добавление</title>
</head>
<body>
    <form asp-action="EmployeeAdd" method="post">
        <div asp-validation-summary="All"></div>
        <div>
            <label asp-for="FullName">ФИО:</label>
            <input asp-for="FullName" placeholder="ФИО" />
        </div>
        <div>
            <label asp-for="Position">Должность:</label>
            <input asp-for="Position" placeholder="Должность" />
        </div>
        <div>
            <label asp-for="DateOfBirth">Дата рождения:</label>
            <input type="datetime-local" asp-for="DateOfBirth" placeholder="Дата рождения" />
        </div>
        <div>
            <label asp-for="Text">Текст:</label>
            <input asp-for="Text" placeholder="Текст" />
        </div>
        <div>
            <label>Декрет?</label>
            <select asp-for="Decree">
                <option value="true">Да</option>
                <option value="false">Нет</option>
            </select>
        </div>
        <div>
            <button type="submit">Добавить</button>
        </div>


    </form>
    <a asp-action="Index" class="btn-dark text-decoration-none">Назад к таблице</a>
</body>
</html>